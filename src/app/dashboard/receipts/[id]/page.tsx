"use client";import { useCallback, useEffect, useMemo, useState } from "react";import { useParams, useRouter } from "next/navigation";import { collection, endBefore, getDoc, getDocs, limit, orderBy, query, startAfter, where } from "firebase/firestore";import { getDownloadURL, ref } from "firebase/storage";import SummaryPanel, { type SummaryPreview } from "./SummaryPanel";import { receiptDoc, storeDoc } from "../../../../lib/firestoreRefs";import { db, storage } from "../../../../lib/firebase/client";import { useUserPermissions } from "../../../../lib/hooks/useUserPermissions";import { canEdit as canEditUtil, canView as canViewUtil, hasStoreAccess as hasStoreAccessUtil } from "../../../../lib/permissions";import type { ReceiptRecord, ReceiptStatus } from "../../../../types/receipt";import { normaliseStoragePath } from "@/lib/utils/storage";const RECEIPTS_FLAG = process.env.NEXT_PUBLIC_APPFLAG_RECEIPTS === "on";const GEMINI_FLAG = process.env.NEXT_PUBLIC_APPFLAG_GEMINI_NORMALIZE === "on";const STATUS_FILTER_OPTIONS: ("all" | ReceiptStatus)[] = ["all", "draft", "pending", "confirmed", "reviewed", "locked"];type ToastKind = "success" | "error" | "info";interface ToastMessage {  id: string;  type: ToastKind;  message: string;}type FilterState = {  storeId: string;  status: "all" | ReceiptStatus;  uploaderId: string;  startDate: string;  endDate: string;};const createDefaultFilters = (receipt: ReceiptRecord | null): FilterState => ({  storeId: receipt?.storeId ?? "",  status: "all",  uploaderId: receipt?.uploaderId ?? "",  startDate: "",  endDate: "",});function normaliseStoragePath(value: string | null | undefined): string {  if (!value) {    return "";  }  if (value.startsWith("gs://")) {    const withoutScheme = value.slice(5);    const slash = withoutScheme.indexOf("/");    if (slash === -1) {      return "";    }    value = withoutScheme.slice(slash + 1);  }  return value.replace(/^\/+/, "").replace(/\/+$/, "");}export default function ReceiptDetailPage(): JSX.Element {  const router = useRouter();  const params = useParams<{ id: string }>();  const receiptId = Array.isArray(params?.id) ? params?.id[0] : params?.id ?? "";  const { permissions, loading: permissionsLoading, authReady } = useUserPermissions();  const [receipt, setReceipt] = useState<ReceiptRecord | null>(null);  const [storeName, setStoreName] = useState<string>("-");  const [loadingReceipt, setLoadingReceipt] = useState(true);  const [error, setError] = useState<string | null>(null);  const [filtersOpen, setFiltersOpen] = useState(false);  const [previewUrl, setPreviewUrl] = useState<string | null>(null);  const [filters, setFilters] = useState<FilterState>(() => createDefaultFilters(null));  const [toasts, setToasts] = useState<ToastMessage[]>([]);const [navigatingPrev, setNavigatingPrev] = useState(false);const [navigatingNext, setNavigatingNext] = useState(false);  const pushToast = useCallback((type: ToastKind, message: string) => {    const id = crypto.randomUUID();    setToasts((prev) => [...prev, { id, type, message }]);    window.setTimeout(() => {      setToasts((prev) => prev.filter((toast) => toast.id !== id));    }, 3000);  }, []);  useEffect(() => {    if (!RECEIPTS_FLAG) {      setLoadingReceipt(false);      setError("Receipts feature is disabled.");      return;    }    if (!authReady || permissionsLoading || !receiptId) {      return;    }    let cancelled = false;    (async () => {      setLoadingReceipt(true);      try {        const snapshot = await getDoc(receiptDoc(receiptId));        if (!snapshot.exists()) {          if (!cancelled) {            setReceipt(null);            setError("Receipt not found.");          }          return;        }        if (cancelled) {          return;        }        const record = { id: snapshot.id, ...(snapshot.data() as ReceiptRecord) };        setReceipt(record);        setFilters(createDefaultFilters(record));        setError(null);      } catch (fetchError) {        console.error("Failed to load receipt", fetchError);        if (!cancelled) {          setError(fetchError instanceof Error ? fetchError.message : "Failed to load receipt.");          setReceipt(null);        }      } finally {        if (!cancelled) {          setLoadingReceipt(false);        }      }    })();    return () => {      cancelled = true;    };  }, [authReady, permissionsLoading, receiptId]);  useEffect(() => {    if (!receipt?.storeId) {      setStoreName("-");      return;    }    let cancelled = false;    (async () => {      try {        const snap = await getDoc(storeDoc(receipt.storeId));        if (!cancelled) {          const data = snap.data() as { name?: string } | undefined;          setStoreName(data?.name?.trim() || receipt.storeId);        }      } catch (storeError) {        console.warn("Failed to load store info", storeError);        if (!cancelled) {          setStoreName(receipt.storeId);        }      }    })();    return () => {      cancelled = true;    };  }, [receipt?.storeId]);  useEffect(() => {    if (!receipt) {      setPreviewUrl(null);      return;    }    const candidates = [receipt.viewPath, receipt.file?.path, receipt.thumbPath, receipt.filePath];    let cancelled = false;    (async () => {      for (const candidate of candidates) {        const normalised = normaliseStoragePath(typeof candidate === "string" ? candidate : undefined);        if (!normalised) {          continue;        }        try {          const url = await getDownloadURL(ref(storage, normalised));          if (!cancelled) {            setPreviewUrl(url);          }          return;        } catch (downloadError) {          console.warn("Failed to load preview", { candidate: normalised, downloadError });        }      }      if (!cancelled) {        setPreviewUrl(null);      }    })();    return () => {      cancelled = true;    };  }, [receipt?.filePath, receipt?.thumbPath, receipt?.viewPath, receipt?.file?.path, receipt?.id]);  useEffect(() => {    if (!filtersOpen) {      return;    }    const handleKeyDown = (event: KeyboardEvent) => {      if (event.key === "Escape") {        setFiltersOpen(false);      }    };    window.addEventListener("keydown", handleKeyDown);    return () => {      window.removeEventListener("keydown", handleKeyDown);    };  }, [filtersOpen]);  useEffect(() => {    setFiltersOpen(false);  }, [receiptId]);  const canViewReceipt = useMemo(() => {    if (!permissions || !receipt) {      return false;    }    return canViewUtil(permissions) && hasStoreAccessUtil(permissions, receipt.storeId);  }, [permissions, receipt]);  const canEditReceipt = useMemo(() => {    if (!permissions || !receipt) {      return false;    }    return canEditUtil(permissions) && hasStoreAccessUtil(permissions, receipt.storeId);  }, [permissions, receipt]);  const isLocked = receipt?.status === "locked";  const uploaderDisplay = useMemo(() => {    const trimmedName = receipt?.uploaderName?.trim();    if (trimmedName) {      return trimmedName;    }    return receipt?.uploaderId ?? "Unknown";  }, [receipt?.uploaderName, receipt?.uploaderId]);  const summaryPreviews: SummaryPreview[] = useMemo(() => {    if (!previewUrl) {      return [];    }    return [      {        id: "receipt-primary",        url: previewUrl,        label: "Receipt",        thumbnailUrl: previewUrl,      },    ];  }, [previewUrl]);  const filtersAtDefault = useMemo(() => {    if (!receipt) {      return true;    }    return (      filters.storeId === (receipt.storeId ?? "") &&      filters.status === "all" &&      filters.uploaderId.trim() === (receipt.uploaderId ?? "") &&      !filters.startDate &&      !filters.endDate    );  }, [filters, receipt]);  const filterStoreMismatch = Boolean(receipt && filters.storeId && filters.storeId !== receipt.storeId);  const filterStatusMismatch = Boolean(receipt && filters.status !== "all" && filters.status !== receipt.status);  const filterUploaderMismatch = Boolean(    receipt && filters.uploaderId.trim() && filters.uploaderId.trim() !== (receipt.uploaderId ?? "")  );  const handleToggleFilters = useCallback(() => {    setFiltersOpen((prev) => !prev);  }, []);  const handleCloseFilters = useCallback(() => {    setFiltersOpen(false);  }, []);  const handleResetFilters = useCallback(() => {    setFilters(createDefaultFilters(receipt));  }, [receipt]);  const handleBackToList = useCallback(() => {    router.push("/dashboard/receipts");  }, [router]);  const handleGoToPrev = useCallback(async () => {    if (!receipt) {      return;    }    const storeId = filters.storeId || receipt.storeId;    const createdAt = receipt.createdAt;    if (!storeId || !createdAt) {      pushToast("info", "No previous receipt.");      return;    }    setNavigatingPrev(true);    try {      const snapshot = await getDocs(        query(          collection(db, "receipts"),          where("storeId", "==", storeId),          orderBy("createdAt", "desc"),          endBefore(createdAt),          limit(1),        ),      );      if (snapshot.empty) {        pushToast("info", "No previous receipt.");        return;      }      router.push(`/dashboard/receipts/${snapshot.docs[0].id}`);    } catch (navigationError) {      console.error("Failed to load previous receipt", navigationError);      pushToast("error", "Failed to load previous receipt.");    } finally {      setNavigatingPrev(false);    }  }, [filters.storeId, receipt, pushToast, router]);  const handleGoToNext = useCallback(async () => {    if (!receipt) {      return;    }    const storeId = filters.storeId || receipt.storeId;    const createdAt = receipt.createdAt;    if (!storeId || !createdAt) {      pushToast("info", "No next receipt.");      return;    }    setNavigatingNext(true);    try {      const snapshot = await getDocs(        query(          collection(db, "receipts"),          where("storeId", "==", storeId),          orderBy("createdAt", "desc"),          startAfter(createdAt),          limit(1),        ),      );      if (snapshot.empty) {        pushToast("info", "No next receipt.");        return;      }      router.push(`/dashboard/receipts/${snapshot.docs[0].id}`);    } catch (navigationError) {      console.error("Failed to load next receipt", navigationError);      pushToast("error", "Failed to load next receipt.");    } finally {      setNavigatingNext(false);    }  }, [filters.storeId, receipt, pushToast, router]);  const handleCancelChanges = useCallback(() => {    pushToast("info", "No pending local changes.");  }, [pushToast]);  const handleSave = useCallback(() => {    pushToast("info", "Changes can be saved from the summary panel.");  }, [pushToast]);  const handleEnhance = useCallback(() => {    pushToast("info", "Enhance action is unavailable in this preview.");  }, [pushToast]);  if (!RECEIPTS_FLAG) {    return (      <div className="flex min-h-[60vh] flex-col items-center justify-center gap-2 p-6">        <h1 className="text-xl font-semibold">Receipts feature is disabled</h1>        <p className="text-sm text-neutral-500">Enable NEXT_PUBLIC_APPFLAG_RECEIPTS to view this page.</p>      </div>    );  }  if (loadingReceipt) {    return (      <div className="flex min-h-[60vh] items-center justify-center p-6">        <p className="text-sm text-neutral-500">Loading receiptÈ©ïÔΩØ„ÉªÅE∂ÁπùÔΩª„ÉªÅE¶</p>      </div>    );  }  if (error) {    return (      <div className="flex min-h-[60vh] flex-col items-center justify-center gap-2 p-6">        <h1 className="text-xl font-semibold">Failed to load receipt</h1>        <p className="text-sm text-neutral-500">{error}</p>      </div>    );  }  if (!receipt) {    return (      <div className="flex min-h-[60vh] flex-col items-center justify-center gap-2 p-6">        <h1 className="text-xl font-semibold">Receipt not found</h1>        <p className="text-sm text-neutral-500">The requested receipt is unavailable.</p>      </div>    );  }  if (!canViewReceipt) {    return (      <div className="flex min-h-[60vh] flex-col items-center justify-center gap-2 p-6">        <h1 className="text-xl font-semibold">Access denied</h1>        <p className="text-sm text-neutral-500">You do not have access to this receipt.</p>      </div>    );  }  return (    <div className="flex flex-col gap-6 p-6">      <header className="flex flex-col gap-3 border-b border-neutral-200 pb-4 lg:flex-row lg:items-center lg:justify-between">        <h1 className="text-2xl font-semibold">Receipt Detail</h1>        <div className="flex flex-wrap items-center gap-4">          <div className="flex flex-wrap gap-2 text-sm text-gray-600">            <span>Store: {storeName}{receipt.storeId ? ` (${receipt.storeId})` : ""}</span>            <span>Status: {receipt.status}</span>            <span>Submitted by: {uploaderDisplay}</span>          </div>          <div className="relative">            <button              type="button"              onClick={handleToggleFilters}              className="rounded border border-neutral-300 px-3 py-1 text-sm font-medium text-neutral-700 hover:bg-neutral-100"            >              Filters            </button>          </div>        </div>      </header>      {filtersOpen ? (        <div className="fixed inset-0 z-40 flex items-center justify-center bg-black/40 px-4" onClick={handleCloseFilters}>          <div            className="w-full max-w-lg rounded-lg border border-neutral-200 bg-white p-4 shadow-lg"            onClick={(event) => event.stopPropagation()}          >            <div className="flex items-center justify-between">              <h2 className="text-sm font-semibold text-neutral-700">Filters</h2>              <button                type="button"                onClick={handleCloseFilters}                className="rounded px-2 py-1 text-xs font-medium text-neutral-600 hover:bg-neutral-100"              >                Close              </button>            </div>            <div className="mt-4 grid gap-3 text-sm sm:grid-cols-2">              <label className="flex flex-col gap-1 text-xs">                <span className="text-neutral-500">Store</span>                <select                  value={filters.storeId}                  onChange={(event) => setFilters((prev) => ({ ...prev, storeId: event.target.value }))}                  className="rounded border border-neutral-300 px-2 py-1 text-sm"                >                  <option value="">All stores</option>                  {[filters.storeId || receipt.storeId]                    .filter(Boolean)                    .map((id) => (                      <option key={id!} value={id!}>                        {id}                      </option>                    ))}                </select>              </label>              <label className="flex flex-col gap-1 text-xs">                <span className="text-neutral-500">Status</span>                <select                  value={filters.status}                  onChange={(event) =>                    setFilters((prev) => ({ ...prev, status: event.target.value as "all" | ReceiptStatus }))                  }                  className="rounded border border-neutral-300 px-2 py-1 text-sm"                >                  {STATUS_FILTER_OPTIONS.map((option) => (                    <option key={option} value={option}>                      {option}                    </option>                  ))}                </select>              </label>              <label className="flex flex-col gap-1 text-xs sm:col-span-2">                <span className="text-neutral-500">Uploader</span>                <input                  type="text"                  value={filters.uploaderId}                  onChange={(event) => setFilters((prev) => ({ ...prev, uploaderId: event.target.value }))}                  className="rounded border border-neutral-300 px-2 py-1 text-sm"                  placeholder={uploaderDisplay || receipt.uploaderId || "Search"}                />                {uploaderDisplay ? (                  <span className="text-[11px] text-neutral-400">Current receipt: {uploaderDisplay}</span>                ) : null}              </label>              <label className="flex flex-col gap-1 text-xs">                <span className="text-neutral-500">Start date</span>                <input                  type="date"                  value={filters.startDate}                  onChange={(event) => setFilters((prev) => ({ ...prev, startDate: event.target.value }))}                  className="rounded border border-neutral-300 px-2 py-1 text-sm"                />              </label>              <label className="flex flex-col gap-1 text-xs">                <span className="text-neutral-500">End date</span>                <input                  type="date"                  value={filters.endDate}                  onChange={(event) => setFilters((prev) => ({ ...prev, endDate: event.target.value }))}                  className="rounded border border-neutral-300 px-2 py-1 text-sm"                />              </label>            </div>            {(filterStoreMismatch || filterStatusMismatch || filterUploaderMismatch) ? (              <div className="mt-4 space-y-1 rounded border border-amber-100 bg-amber-50 p-2 text-xs text-amber-700">                <p>Current receipt might fall outside the active filters.</p>                {filterStoreMismatch ? <p>Store: {receipt.storeId}</p> : null}                {filterStatusMismatch ? <p>Status: {receipt.status}</p> : null}                {filterUploaderMismatch ? <p>Uploader ID: {receipt.uploaderId}</p> : null}              </div>            ) : null}            <div className="mt-4 flex items-center justify-between">              <button                type="button"                onClick={handleResetFilters}                disabled={filtersAtDefault}                className="rounded border border-neutral-300 px-3 py-1 text-xs font-medium text-neutral-700 hover:bg-neutral-50 disabled:cursor-not-allowed disabled:opacity-50"              >                Reset              </button>              <button                type="button"                onClick={handleCloseFilters}                className="rounded bg-blue-600 px-3 py-1 text-xs font-medium text-white hover:bg-blue-700"              >                Apply              </button>            </div>          </div>        </div>      ) : null}      <SummaryPanel        receipt={receipt}        canEdit={canEditReceipt && !isLocked}        pushToast={pushToast}        onReceiptUpdate={setReceipt}        previews={summaryPreviews}      />      <div className="flex flex-wrap gap-3">        <button          type="button"          onClick={handleBackToList}          className="rounded border border-neutral-300 px-4 py-2 text-sm font-medium text-neutral-700 hover:bg-neutral-100"        >          Back to list        </button>        <button          type="button"          onClick={handleGoToPrev}          disabled={navigatingPrev}          className="rounded border border-neutral-300 px-4 py-2 text-sm font-medium text-neutral-700 hover:bg-neutral-100 disabled:cursor-not-allowed disabled:opacity-50"        >          {navigatingPrev ? "Loading..." : "Previous"}        </button>        <button          type="button"          onClick={handleGoToNext}          disabled={navigatingNext}          className="rounded border border-neutral-300 px-4 py-2 text-sm font-medium text-neutral-700 hover:bg-neutral-100 disabled:cursor-not-allowed disabled:opacity-50"        >          {navigatingNext ? "Loading..." : "Next"}        </button>        <button          type="button"          onClick={handleCancelChanges}          className="rounded border border-neutral-300 px-4 py-2 text-sm font-medium text-neutral-700 hover:bg-neutral-100"        >          Cancel changes        </button>        <button          type="button"          onClick={handleSave}          className="rounded bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700"        >          Confirm        </button>        {GEMINI_FLAG && !isLocked ? (          <button            type="button"            onClick={handleEnhance}            className="rounded border border-purple-400 px-4 py-2 text-sm font-medium text-purple-700 hover:bg-purple-50"          >            Enhance with AI          </button>        ) : null}      </div>      <div className="fixed inset-x-0 bottom-4 flex justify-center">        <div className="flex w-full max-w-sm flex-col gap-2">          {toasts.map((toast) => (            <div              key={toast.id}              className={`rounded border px-3 py-2 text-sm shadow ${                toast.type === "success"                  ? "border-green-200 bg-green-50 text-green-800"                  : toast.type === "error"                  ? "border-red-200 bg-red-50 text-red-800"                  : "border-neutral-200 bg-neutral-50 text-neutral-700"              }`}            >              {toast.message}            </div>          ))}        </div>      </div>    </div>  );}